const AWS = require("aws-sdk");
const fs = require('fs');
const readline = require('readline');

const express = require("express");
const router = express.Router();

async function buscarDados(mac, linhas){

    console.log(AWS)
    
}